{
  "schemaVersion": 1,
  "id": "corruptconfigannihilator",
  "version": "${version}",
  "name": "CorruptConfigAnnihilator",
  "description": "Default configuration provisioning that heals corrupt configs, rolls out pack updates to untouched files, and enforces overrides. Everything it replaces is backed up.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "preLaunch": [
      "dev.autism.prominence.annihilator.Annihilator"
    ]
  },
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "~1.20.1",
    "java": ">=17"
  }
}
